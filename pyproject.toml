[project]
name = "aiscraper"
version = "0.2.0"
description = "AI-driven content scraper for archives and files"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
    "py7zr>=1.1",
    "pyzipper>=0.3",
]

[project.scripts]
aiscraper = "main:main"
