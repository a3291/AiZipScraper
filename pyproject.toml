[project]
name = "archive-scraper"
version = "0.1.0"
description = "AI-powered scraper for archives and files"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
    "py7zr>=1.1",
    "pyzipper>=0.3",
]

[project.scripts]
scraper = "main:main"
